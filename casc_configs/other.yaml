unclassified:
  themeManager:
    disableUserThemes: true
    theme: "dark" 

  location:
    url: http://localhost
    adminAddress: admin@admin.com

  globalLibraries:
    libraries:
      - name: "hci"
        implicit: true
        defaultVersion: "main"
        retriever:
          modernSCM:
            scm:
              git:
                remote: "https://github.com/HomeCI/jenkins.git"
                